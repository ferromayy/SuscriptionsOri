-- Run AFTER creating the Super Admin user in Supabase Auth (Authentication > Users).
-- Replace the UUID and email below with your real super admin user.

-- Example:
-- insert into public.platform_admins (user_id)
-- values ('00000000-0000-0000-0000-000000000001')
-- on conflict (user_id) do nothing;
